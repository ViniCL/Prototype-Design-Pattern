O Design Pattern Prototype é basicamente um padrão que permite poder clonar um objeto já existente, sem mistério.
Dessa forma (clonando) conseguimos um novo objeto muito mais facilmente, pois evitamos a necessidade de criar um novo objeto do zero. 
É muito útil para ser usado quando queremos um novo objeto que é complexo, ou seja, que necessita de inúmeros procedimentos para ser formado, por exemplo.  

Explicação do Código:
prototype.h -> É a interface que contém o método de clonagem. As classes que poderão ser clonadas devem implementar sua funcionalidade; 
car.cpp e car.h -> Exemplo simples de classe para criação de objetos. O importante aqui é observamos a herança da classe Prototype e a implementação do método Clone(), dessa forma todos os objetos tipo Car poderão ser clonados, simplesmente chamando esse método.   
main.h -> Está mostrando como ocorre a clonagem do objeto. 
